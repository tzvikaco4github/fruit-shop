apply plugin: "war"

bootWar {
    mainClassName = "net.tzvikaco.fruitshop.FruitShopApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
